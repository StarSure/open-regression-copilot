version: "3.9"

services:
  aitestautomate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aitestautomate
    ports:
      - "4318:4318"
    environment:
      PORT: 4318
      HOST: 0.0.0.0
    volumes:
      - ./.data:/app/.data
    restart: unless-stopped
